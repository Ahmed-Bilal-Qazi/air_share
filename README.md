# air_share
A lightweight, local network file-sharing tool built with Flask. It lets you transfer files between devices on the same WiFi—no accounts, no cloud, no setup beyond running a single script.  It works by spinning up a small web server on your machine and exposing a simple interface accessible via browser. 
